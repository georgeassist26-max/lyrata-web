// LeadWidget.tsx
// Isla Activa: botón flotante / widget de captura de leads → WhatsApp API
// Hidratación recomendada: client:idle

export default function LeadWidget() {
  return (
    <div id="lead-widget">
      {/* Placeholder: implementación de widget flotante → WhatsApp API */}
    </div>
  );
}
